# Copy to .env on the VM (same directory as docker-compose.prod.yml)

# Image tag: latest or a commit SHA from GHCR
IMAGE_TAG=latest

# Public HTTP port (80 needs root, or use 8080)
HTTP_PORT=80

# Optional — real OpenAI instead of mock providers
OPENAI_API_KEY=
