name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  backend-test:
    name: Backend tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run pytest in Docker
        run: docker compose --profile test run --rm backend-test

  frontend-build:
    name: Frontend build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build frontend image
        run: docker build -t company-docs-rag-frontend ./frontend

  integration:
    name: Integration smoke test
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-build]
    steps:
      - uses: actions/checkout@v4

      - name: Start stack
        run: docker compose up --build -d

      - name: Wait for API health
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8080/api/health; then
              echo ""
              echo "Stack is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "Stack failed to become healthy"
          docker compose logs
          exit 1

      - name: Seed smoke-test document
        run: |
          curl -sf -X POST http://localhost:8080/api/admin/reset
          curl -sf -X POST http://localhost:8080/api/ingest/text \
            -H "Content-Type: application/json" \
            -d '{"source_name":"ci-smoke.txt","text":"Full-time employees receive 20 paid vacation days per year."}'
          count=$(curl -sf http://localhost:8080/api/health | python3 -c "import sys, json; print(json.load(sys.stdin)['documents_indexed'])")
          echo "Indexed chunks: $count"
          test "$count" -gt 0

      - name: Smoke test RAG query
        run: |
          python3 <<'PY'
          import json
          import urllib.request

          req = urllib.request.Request(
              "http://localhost:8080/api/query",
              data=json.dumps(
                  {"question": "How many vacation days do employees receive?"}
              ).encode(),
              headers={"Content-Type": "application/json"},
              method="POST",
          )
          body = json.load(urllib.request.urlopen(req))

          assert body["grounded"] is True, body
          assert len(body["citations"]) > 0, body
          assert any("20" in c.get("excerpt", "") for c in body["citations"]), body

          print("Smoke test passed")
          print(body["answer"][:120])
          PY

      - name: Tear down
        if: always()
        run: docker compose down -v
